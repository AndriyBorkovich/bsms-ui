export enum Role {
  Admin = 100,
  Passenger = 200,
  Driver = 300
}

export enum Direction {
  ToDestination = 100,
  FromDestination = 200
}

// mirrors .NET system enum
export enum DayOfWeek {
    Sunday = 0,
    Monday = 1,
    Tuesday = 2,
    Wednesday = 3,
    Thursday = 4,
    Friday = 5,
    Saturday = 6
}